import * as React from "react";
import { StyleSheet, View } from "react-native";

const TeenyiconsshopOutline = () => {
  return <View style={styles.teenyiconsshopOutline} />;
};

const styles = StyleSheet.create({
  teenyiconsshopOutline: {
    width: 15,
    height: 15,
    overflow: "hidden",
  },
});

export default TeenyiconsshopOutline;
